static void pushDisplay(const char* line0,
                        const char* line1,
                        int cursor,
                        const Icons& ic,
                        int bank = -1,
                        int preset = -1);
